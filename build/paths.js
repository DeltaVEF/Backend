let appRoot = 'src/';
let outputRoot = 'dist/';

export let paths = {
    compiledServer: `${outputRoot}server.js`,
    delete: `${outputRoot}**/*.js`,
	jsonOutput: `${outputRoot}`,
	jsonSource: `${appRoot}**/*.json`,
    output: `${outputRoot}`,
    source: `${appRoot}**/*.js`
}
