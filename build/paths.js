let appRoot = 'src/';
let outputRoot = 'dist/';

export let paths = {
    compiledServer: `${outputRoot}server.js`,
    delete: `${outputRoot}**/*.js`,
    output: `${outputRoot}`,
    source: `${appRoot}**/*.js`
}
